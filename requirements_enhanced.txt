# Core dependencies for enhanced features
matplotlib>=3.5.0
numpy>=1.24.0
scipy>=1.10.0

# Note: asyncio, datetime, decimal, collections are built-in Python modules
# Note: tkinter is a system package and should not be installed via pip
