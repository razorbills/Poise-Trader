# Minimal dependencies for FREE cloud hosting
# Optimized for PythonAnywhere, Replit, Render, etc.

# Core async and HTTP
aiohttp>=3.8.0
websockets>=11.0
requests>=2.28.0

# Data processing (minimal)
pandas>=1.5.0
numpy>=1.24.0

# Technical analysis
ta>=0.10.2

# Exchange API
ccxt>=4.0.0

# Database (lightweight)
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Security (basic)
cryptography>=41.0.0

# System monitoring (lightweight)
psutil>=5.9.0

# Configuration
python-dotenv>=1.0.0
colorama>=0.4.6
rich>=13.0.0

# Note: Heavy ML libraries (tensorflow, torch, sklearn) excluded for free hosting
# The bot's AI system will work with reduced ML features
